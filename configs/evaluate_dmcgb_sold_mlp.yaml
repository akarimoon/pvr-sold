defaults:
  - hydra: default

checkpoint_path: "../checkpoints/sold/dmcgb_finger_spin/r78k8wwc.ckpt"  # Can be one checkpoint or a directory of checkpoints.
experiment: "eval_dmcgb_sold"
eval_episodes: 5
seed: 42

env:
  _target_: envs.make_env
  suite: dmcgb  # Should be "mof", "gym", or "dmcontrol".
  name: finger_spin
  image_size: [ 64, 64 ]
  max_episode_steps: 50
  action_repeat: 2
  mode: color_easy_test
  use_cls_token: false
  freeze_vit: false
  vit_cache_dir: null
  vit_model: null
  seed: null
